# ==========================================
# ОЧЕРЕДИ ОРКЕСТРАТОРА - Настройки пропускной способности
# ==========================================

# === UPSCALE ОЧЕРЕДИ ===

# 1. Очередь загрузки файлов на GPU (1 одновременно)
UPSCALE_UPLOAD_CONCURRENCY=1

# 2. Очередь обработки на GPU (2 одновременно) 
UPSCALE_CONCURRENCY=2

# 3. Очередь скачивания результатов с GPU (1 одновременно)
UPSCALE_RESULT_DOWNLOAD_CONCURRENCY=1

# === CUT ОЧЕРЕДИ ===
# Cut очереди используют фиксированную конфигурацию:
# - download_queue: 1 worker (встроено в код)
# - process_queue: 1 worker (встроено в код)
# Никаких дополнительных настроек не требуется

# ==========================================
# GPU SERVER CONNECTION
# ==========================================

# Прямой HTTP endpoint GPU сервера (если используете)
VAST_UPSCALE_URL=http://YOUR_GPU_IP:5000

# SSH параметры для GPU сервера (если используете)
VAST_SSH_HOST=YOUR_GPU_IP
VAST_SSH_PORT=22
VAST_SSH_USER=root
VAST_SSH_KEY=/path/to/your/ssh/key

# ==========================================  
# CUT PROCESSING SETTINGS
# ==========================================

# Обрабатывать Cut на GPU сервере (рекомендуется)
CUT_ON_GPU=1

# Базовая директория на GPU сервере для Cut
VAST_CUT_BASE_DIR=/workspace/cut

# Размер модели Whisper
WHISPER_MODEL=small

# OpenAI API ключ для GPT-обработки
OPENAI_API_KEY=your_openai_key_here

# ==========================================
# CORS для Next.js фронтенда  
# ==========================================
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000